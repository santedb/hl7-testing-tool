name: Build Ubuntu

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Setup .NET
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: 3.1.417
    - name: Restore dependencies
      run: dotnet restore HL7TestingTool.sln
    - name: Build
      run: dotnet build HL7TestingTool.sln
